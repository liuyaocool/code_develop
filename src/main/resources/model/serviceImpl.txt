package #servicePkg#;

import com.github.pagehelper.PageHelper;
import com.github.pagehelper.PageInfo;
import #entityImport#;
import #iServiceImport#;
import #iMapperImport#;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.context.annotation.Primary;
import org.springframework.stereotype.Service;

@Service
@Primary
public class #fileName# implements #iServiceName#{

	@Autowired
	private #iMapperName# baseMapper;

    @Override
    public int deleteByPrimaryKey(String primaryId){
        return baseMapper.deleteByPrimaryKey(primaryId);
    };

    @Override
    public int insert(#filePrefix# record){
        return baseMapper.insert(record);
    };

    @Override
    public #filePrefix# selectByPrimaryKey(String primaryId){
        return baseMapper.selectByPrimaryKey(primaryId);
    };

    @Override
    public PageInfo<#filePrefix#> selectAll(int pageNum, int pageSize){
        PageHelper.startPage(pageNum,pageSize);
        return new PageInfo<>(baseMapper.selectAll());
    };

    @Override
    public int updateByPrimaryKey(#filePrefix# record){
        return baseMapper.updateByPrimaryKey(record);
    };
}